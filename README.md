# remoteapp

This repository includes a simple command-line tool `add_numbers.py` for adding numbers.

## Usage

Run the script with numbers as arguments:

```bash
python add_numbers.py 1 2 3
```

This will output `6`.

## Testing

Unit tests can be executed with:

```bash
python -m unittest
```
